# Ajuste de curvas

Pasta reservada para o método dos mínimos quadrados.

Dataset relacionado:

- `datasets/04_trafego_rede_mmq.csv`

Objetivo futuro:

- encontrar a reta `P1(x) = ax + b`;
- prever o tráfego para `x = 13 h`.
